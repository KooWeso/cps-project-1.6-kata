const path = require('path');

module.exports = {
    mode: 'development',
    output: {
        filename: 'webpack.bundle.js',
    },
    modules: {
        rules: [
            
        ],
    },
    plugins: [

    ],
}